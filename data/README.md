# Place your heart disease CSV dataset here.
#
# Example: heart.csv
#
# The training pipeline auto-detects the target column.
# Supported target column names: target, label, class, output, diagnosis, etc.
# If auto-detection fails, specify it manually:
#
#   python train.py --data data/heart.csv --target your_column
